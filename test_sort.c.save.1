#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[]) {
    int *arr;
    int size = atoi(argv[4]);
    
    arr = (int*)malloc(sizeof(int)*size);
    
    if(strcmp(same,argv[3])){
        printf("hello");
    }
    else if(strcmp(argv[3],asc)){
    
    }
    else if(strcmp(argv[3],desc)){
    
    }
    else if(strcmp(argv[3],rand)){
    
    }

    return 0;
}
(strcmp(same,argv[3]))
